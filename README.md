# clock-project
This project implements a WiFi-synchronized digital clock using an ESP-32 and aMAX7219-driven 7-segment display The ESP connects to the internet, retrieves the current time from an NTP server, and updates the display in real time via SPI communication.
